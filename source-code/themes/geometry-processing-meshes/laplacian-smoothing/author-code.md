# Author Code Notes

This production starter is original toy/reference code. It is not copied from a
SIGGRAPH paper implementation.

Use paper author repositories as references only after checking:

- license
- data/model size
- reproducible install
- whether code can be wrapped without vendoring

